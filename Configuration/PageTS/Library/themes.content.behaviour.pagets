themes {
	content {
		# Behaviour for content elements
		behaviour {
			default {
				container = Container
				fade-in = Fade in
				#parallax-01 = Parallax
			}
			table {
				table-responsive = Responsive table
			}
			gridelements_pi1 {
				nav-pills = Pills navigation
				nav-tabs = Tabs navigation
			}
		}
	}
}