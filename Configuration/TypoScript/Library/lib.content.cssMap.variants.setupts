lib.content.cssMap.variants {
	
	# Variants available for each element
	variants-alert-success = alert alert-success
	variants-alert-info = alert alert-info
	variants-alert-warning = alert alert-warning
	variants-alert-danger = alert alert-danger
	variants-showcase = jumbotron
	variants-framed-box = well
	variants-framed-box-small = well well-sm
	variants-framed-box-large = well well-lg

	# Variants only for header content elements
	variants-page-header = page-header
}
