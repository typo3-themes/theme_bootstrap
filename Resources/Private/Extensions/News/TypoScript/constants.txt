# cat=container; type=int+; label= Container for news (tt_news, news). Default is theme rootpage.
themes.configuration.container.news = {$themes.templatePageId}

# cat=pages; type=int+; label= News list page. Default is theme rootpage.
themes.configuration.pages.newsList = {$themes.templatePageId}

# cat=pages; type=int+; label= News detail page.  Default is theme rootpage.
themes.configuration.pages.newsDetails = {$themes.templatePageId}
